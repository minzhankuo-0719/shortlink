# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# Django
*.log
db.sqlite3
db.sqlite3-journal
/staticfiles/
/media/

# Environment / secrets (commit only the example)
.env
.env.*
!.env.example

# OS / editor
.DS_Store
.idea/
.vscode/

# Claude Code local settings (personal, not shared)
.claude/settings.local.json

# Personal notes kept local only — out of version control to keep the repo's
# current view clean (git history still has them). deployment.md stays tracked.
CLAUDE.md
docs/adr/
docs/interview-qa.md
docs/learning-log.md

# Note: uv.lock IS committed (reproducible builds).
